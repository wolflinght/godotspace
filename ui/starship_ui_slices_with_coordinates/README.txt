This package contains the UI slices with source coordinates prefixed in each filename.

Filename format:
  x####_y####_w####_h####_<original_name>.png

Meaning:
  x, y = top-left position in the original source image
  w, h = crop width and height

Additional files:
  - coordinates_manifest.csv
  - manifest_with_coordinates.json
